/**
 * Middleware Module Re-export
 * 
 * This file re-exports the middleware implementation to maintain backward compatibility
 * with existing imports while avoiding duplicate middleware detection.
 */

export * from './implementation'; 